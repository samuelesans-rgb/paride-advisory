import { CtaBanner } from "@/components/cta-banner";
import { PageHero } from "@/components/page-hero";
import { SectionHeading } from "@/components/section-heading";
import { networkProfessionals } from "@/lib/site-data";
import { createMetadata } from "@/lib/seo";
export const metadata = createMetadata({ title: "Network professionisti", description: "Una rete di professionisti qualificati per risposte multidisciplinari.", path: "/network" });
export default function NetworkPage() { return <><PageHero eyebrow="Network professionisti" title="Una rete qualificata, coordinata intorno alle tue esigenze." description="Quando un progetto richiede competenze specifiche, attiviamo i professionisti più adatti e ne coordiniamo il contributo." /><section className="container grid gap-12 py-20 sm:py-28 lg:grid-cols-2"><SectionHeading eyebrow="Competenze connesse" title="Un sistema di professionalità al servizio dei risultati." description="Lavoriamo in sinergia per offrire una risposta organica, con un referente che mantiene il quadro d’insieme." /><div className="grid gap-px border border-slate-200 bg-slate-200 sm:grid-cols-2">{networkProfessionals.map((item, index) => <div className="bg-white p-5" key={item}><span className="text-xs font-bold text-gold">{String(index + 1).padStart(2, "0")}</span><p className="mt-5 font-semibold text-navy">{item}</p></div>)}</div></section><section className="bg-slate-50 py-16"><div className="container max-w-4xl border-l-2 border-gold py-2 pl-7 text-lg leading-8 text-navy">Le attività soggette a specifiche abilitazioni professionali vengono svolte esclusivamente dai professionisti competenti e regolarmente abilitati.</div></section><CtaBanner /></>; }
