import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowUpRight, Check } from "lucide-react";
import { Breadcrumbs } from "@/components/breadcrumbs";
import { CtaBanner } from "@/components/cta-banner";
import { articles } from "@/lib/articles";
import { authorityPages, sectorPages } from "@/lib/authority-data";
import { createMetadata } from "@/lib/seo";
import { services, siteConfig } from "@/lib/site-data";

const relations: Record<string, { services: string[]; competences: string[]; sector: string; article: string }> = {
  amministrativa: { services: ["finanziaria", "strategica"], competences: ["controllo-di-gestione", "analisi-dei-costi", "procedure-amministrative"], sector: "pmi", article: "controllo-di-gestione-pmi-redditivita" },
  finanziaria: { services: ["amministrativa", "strategica"], competences: ["pianificazione-finanziaria", "cash-flow", "business-plan", "budget-aziendale"], sector: "pmi", article: "cash-flow-aziendale-monitorare-liquidita" },
  strategica: { services: ["amministrativa", "project-management"], competences: ["organizzazione-aziendale", "ottimizzazione-dei-processi", "riorganizzazione-aziendale", "nuove-attivita-e-startup", "digitalizzazione-dei-processi"], sector: "attivita-commerciali", article: "business-plan-cosa-contiene-quando-serve" },
  tecnici: { services: ["project-management", "strategica"], competences: ["facility-management", "project-management"], sector: "condomini", article: "project-management-riqualificazione" },
  "project-management": { services: ["tecnici", "strategica"], competences: ["project-management", "facility-management", "ottimizzazione-dei-processi"], sector: "condomini", article: "project-management-riqualificazione" },
};
const serviceHeroImages: Record<string, { src: string; alt: string; position: string }> = {
  amministrativa: { src: "/images/home/consulenza-amministrativa-documenti.webp", alt: "Consulenti verificano e compilano documenti amministrativi", position: "44% center" },
  finanziaria: { src: "/images/home/consulenza-finanziaria-grafici.webp", alt: "Analisi di report e grafici per la pianificazione finanziaria", position: "58% center" },
  strategica: { src: "/images/home/consulenza-strategica-riunione.webp", alt: "Gruppo di professionisti discute dati e priorità strategiche", position: "center center" },
  tecnici: { src: "/images/home/metodologia-consulenza-ufficio.webp", alt: "Ambiente di lavoro organizzato per una consulenza tecnica", position: "center center" },
  "project-management": { src: "/images/home/project-management-sala-riunioni.webp", alt: "Sala riunioni organizzata per il coordinamento di un progetto", position: "center center" },
};

export const dynamicParams = false;
export function generateStaticParams() { return services.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const service = services.find((item) => item.slug === slug); return service ? createMetadata({ title: service.title, description: service.summary, path: `/servizi/${service.slug}` }) : {}; }
export default async function ServicePage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params; const service = services.find((item) => item.slug === slug); if (!service) notFound(); const relation = relations[slug]; const Icon = service.icon; const relatedServices = relation.services.map((item) => services.find((entry) => entry.slug === item)!); const competences = relation.competences.map((item) => authorityPages.find((entry) => entry.slug === item)!); const sector = sectorPages.find((item) => item.slug === relation.sector)!; const article = articles.find((item) => item.slug === relation.article)!; const url = siteConfig.url + "/servizi/" + slug; const heroImage = serviceHeroImages[slug];
  const jsonLd = [{ "@context": "https://schema.org", "@type": "Service", name: service.title, description: service.description, provider: { "@type": "ProfessionalService", name: siteConfig.name, url: siteConfig.url }, areaServed: ["Milano", "Italia"], url }, { "@context": "https://schema.org", "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Home", item: siteConfig.url }, { "@type": "ListItem", position: 2, name: "Servizi", item: `${siteConfig.url}/servizi` }, { "@type": "ListItem", position: 3, name: service.title, item: url }] }];
  return <><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Servizi", href: "/servizi" }, { label: service.title }]} /><section className="bg-slate-50 py-16 sm:py-24"><div className="container grid items-center gap-12 lg:grid-cols-[1.05fr_.95fr]"><div><p className="eyebrow">{service.eyebrow}</p><h1 className="mt-5 max-w-4xl text-4xl font-semibold tracking-[-.055em] text-navy sm:text-6xl">{service.title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-600">{service.description}</p><Link className="button-primary mt-8" href="/contatti">Richiedi un primo confronto <ArrowUpRight className="size-4" /></Link></div><div className="relative aspect-[16/9] min-h-64 overflow-hidden bg-navy"><Image src={heroImage.src} alt={heroImage.alt} width={1672} height={941} sizes="(min-width: 1280px) 560px, (min-width: 1024px) 45vw, 100vw" loading="lazy" className="absolute inset-0 h-full w-full object-cover" style={{ objectPosition: heroImage.position }} /></div></div></section><section className="container grid gap-12 py-20 sm:py-28 lg:grid-cols-[.75fr_1.25fr]"><div><Icon className="size-12 text-gold" strokeWidth={1.25} /><h2 className="mt-6 text-3xl font-semibold tracking-[-.04em] text-navy">Un supporto costruito sulle priorità dell’impresa.</h2>{service.note && <p className="mt-6 border-l-2 border-gold bg-slate-50 p-5 text-sm leading-6 text-slate-700">{service.note}</p>}</div><div><p className="eyebrow">Ambiti di intervento</p><ul className="mt-7 grid gap-4 sm:grid-cols-2">{service.features.map((feature) => <li className="flex gap-3 border-t border-slate-200 py-4 text-sm text-navy" key={feature}><Check className="size-5 shrink-0 text-gold" />{feature}</li>)}</ul><Link className="button-secondary mt-8" href={`/competenze/${competences[0].slug}`}>Approfondisci {competences[0].title.toLowerCase()}</Link></div></section><section className="bg-navy py-20 text-white"><div className="container"><p className="eyebrow">Competenze specifiche</p><h2 className="mt-4 max-w-3xl text-3xl font-semibold tracking-[-.04em]">Approfondimenti collegati, senza perdere la visione d’insieme.</h2><div className="mt-10 grid gap-3 md:grid-cols-2 lg:grid-cols-3">{competences.map((item) => <Link className="group flex min-h-44 flex-col justify-between border border-white/15 p-5 transition hover:border-gold focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gold" href={`/competenze/${item.slug}`} key={item.slug}><span className="text-xs font-bold uppercase tracking-widest text-gold">Competenza</span><span className="flex items-end justify-between gap-3 font-semibold">{item.title}<ArrowUpRight className="size-4" /></span></Link>)}</div></div></section><section className="container py-20 sm:py-28"><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Related label="Tutti i servizi" title="Panoramica servizi" href="/servizi" />{relatedServices.map((item) => <Related label="Servizio correlato" title={item.title} href={`/servizi/${item.slug}`} key={item.slug} />)}<Related label="Settore" title={sector.title} href={`/settori/${sector.slug}`} /><Related label="Insight" title={article.title} href={`/blog/${article.slug}`} /></div></section><CtaBanner /><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /></>;
}
function Related({ label, title, href }: { label: string; title: string; href: string }) { return <Link className="group flex min-h-44 flex-col justify-between border border-slate-200 p-5 text-navy transition hover:border-gold focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gold" href={href}><span className="eyebrow">{label}</span><span className="flex items-end justify-between gap-3 font-semibold">{title}<ArrowUpRight className="size-4 shrink-0" /></span></Link>; }
