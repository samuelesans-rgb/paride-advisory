import { Mail, MessageCircle, Phone } from "lucide-react";
import { ContactForm } from "@/components/contact-form";
import { PageHero } from "@/components/page-hero";
import { siteConfig } from "@/lib/site-data";
import { createMetadata } from "@/lib/seo";
export const metadata = createMetadata({ title: "Contatti", description: "Contatta Paride Sansò Advisory per una consulenza dedicata.", path: "/contatti" });
export default function ContactPage() { return <><PageHero eyebrow="Contatti" title="Parliamo della tua prossima priorità." description="Inviaci la tua richiesta: ti ricontatteremo per approfondire esigenze e possibili soluzioni." /><section className="container grid gap-14 py-20 sm:py-28 lg:grid-cols-[.8fr_1.2fr]"><aside><p className="eyebrow">Contatti diretti</p><h2 className="mt-4 text-3xl font-semibold tracking-[-0.04em] text-navy">Scegli il canale che preferisci.</h2><div className="mt-9 space-y-5">{[[Phone, "Telefono", siteConfig.phone, siteConfig.phoneHref], [Mail, "Email", siteConfig.email, `mailto:${siteConfig.email}`], [MessageCircle, "WhatsApp", "Scrivici su WhatsApp", siteConfig.whatsapp]].map(([Icon, label, value, href]) => { const ContactIcon = Icon as typeof Phone; return <a className="flex items-center gap-4 text-sm text-navy" href={href as string} key={label as string}><span className="flex size-10 items-center justify-center bg-slate-100"><ContactIcon className="size-5 text-gold" /></span><span><strong className="block">{label as string}</strong><span className="text-slate-600">{value as string}</span></span></a>; })}</div></aside><div className="border border-slate-200 bg-slate-50 p-6 sm:p-9"><h2 className="text-2xl font-semibold tracking-[-0.03em] text-navy">Richiedi una consulenza</h2><p className="mt-2 text-sm text-slate-600">I campi contrassegnati sono necessari per gestire la tua richiesta.</p><div className="mt-8"><ContactForm /></div></div></section></>; }
